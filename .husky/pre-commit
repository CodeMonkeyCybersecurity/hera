#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "Running pre-commit checks..."

# Run lint-staged to check only staged files
npx lint-staged

# Run unit tests only (fast) — full coverage runs in CI
echo "Running unit tests..."
npm run test:unit

echo "Pre-commit checks passed!"
